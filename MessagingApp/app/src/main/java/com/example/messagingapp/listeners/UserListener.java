package com.example.messagingapp.listeners;

import com.example.messagingapp.models.User;

public interface UserListener {
    // Interface method to handle user click events
    void onUserClicked(User user);
}
